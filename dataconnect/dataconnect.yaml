specVersion: "v1"
serviceId: "consultapp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "consultapp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
